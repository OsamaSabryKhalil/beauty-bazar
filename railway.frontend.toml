[build]
  builder = "nixpacks"
  buildCommand = "cd client && npm install && npm run build"

[deploy]
  startCommand = "cd client/dist && npx serve -s"
  healthcheckPath = "/"
  healthcheckTimeout = 100
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 10 